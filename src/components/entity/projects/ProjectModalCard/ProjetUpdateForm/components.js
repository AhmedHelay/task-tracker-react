import makeStyles from '@material-ui/styles/makeStyles'

export const useStyle = makeStyles(() => ({
  root: {
    padding: '10px'
  },

  button: {
    fontWeight: 'bold',
    marginTop: '10px',
    width: '200px'
  }
}))
